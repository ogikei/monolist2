<% content_for :user_ogp do %>
    <meta property="og:title" content="<%= @user.name %> - monolist" description=<%= @user.name %>"のモノページ。Hanakoの欲しいモノ、持ってるモノを表示しています。" />
<% end %>

<%= render 'profile' , user: @user %>

<%= render 'items/list' , items: @items %>
